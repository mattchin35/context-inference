Code for raw data analysis

Preprocessing:
preprocess_session.py - read raw session logs and create cleaned dataframe files in session_events. Consider updating
    the processed output to match the external folder where my files are held (the LabComputerShare dropbox folder)

session_overview.py - code for creating the cleaned dataframe files and displaying water delivery. preprocess_session.py
    uses these functions; running them from session overview is exactly the same.
